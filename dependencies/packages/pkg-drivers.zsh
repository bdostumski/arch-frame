#!/usr/bin/env zsh
#
# Drivers
#

export PACMAN_PACKAGES=(

    # Printer
    cups
    system-config-printer
    foomatic-db
    foomatic-db-engine
    ghostscript

    # Nvidia
    nvidia
    nvidia-utils
    nvidia-settings
    cuda
)
